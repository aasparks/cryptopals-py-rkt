#lang racket/base

(require "../set1/c1.rkt"
         "../set1/c2.rkt")

(provide ascii->hex
         hex->ascii
         ascii->base64
         base64->ascii
         hex->base64
         base64->hex
         xorstrs)